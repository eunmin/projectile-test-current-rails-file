projectile-test-current-rails-file
==================================

Run a single rails unit test in projectile project

Installation
============
```
(load "~/.emacs.d/projectile-test-current-rails-file.el")
(global-set-key (kbd "C-c p P") 'projectile-test-current-rails-file)
```
